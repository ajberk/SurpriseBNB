
LAST FEATURES


-Make Users show page This will have a list of users upcoming vacations
This list will include dates your gonna travel, the title, address and price per night.
Should be sorted by start date of vacation. - make a bookings table that will fill in when user hits submit


-make nav bar and footer orange.



BUGS
-still some map problems???



<%= this.listings.getOrFetch(booking.escape("listing_id").escape("street address")%>,
        <br>
        <%= this.listings.getOrFetch(booking.escape("listing_id").escape("country") %>,
        <%= this.listings.getOrFetch(booking.escape("listing_id").escape("zipcode") %>

        <h1 class="list-group-item-heading"><%= this.listings.getOrFetch(booking.escape("listing_id").escape("title") %></h1>
